# HANDOFF — RTG Hub · 2026-09-04

Lee esto completo. Luego VERIFICA el estado real antes de confiar en nada de
aquí (sección 0).

## 0. VERIFICA PRIMERO (no confíes en este documento a ciegas)

- `git log --oneline -10` · `git status` · versión en `package.json` y APP_VERSION.
- `node scripts/db/migrate-status.mjs` → debe decir "todo al día".
- `npx vitest run` → reporta SIEMPRE "pasados | saltados" (hoy 704 | 3 = 707).
- Lee `CLAUDE.md`, la cola de `DECISIONS.md` (desde D-172), `docs/AUDIT-2026-09.md`
  (incluida su sección de cierre) y `ARCHITECTURE.md` §11 y §ERP.

Si algo de aquí contradice lo que mides, gana lo que mides — y dilo.

## 1. QUÉ ES

RTG Hub: UNA app Next.js 14 (App Router) + Supabase, repo
`codingcodertg/DELIVERY-APP`, carpeta `C:\Users\andre\Documents\CLAUDE\DELIVERIES APP\rtg-hub` (antes `deliveries-app`; paso 11 de docs/PLAN-rename-rtg-hub.md),
producción `rtg-hub.vercel.app` (antes `deliveries-app-seven.vercel.app`, que desde el 2026-09-04 redirige con 307), proyecto Supabase
`iwhcsvgujydebdyllcqu`. Cuatro módulos sobre UNA identidad compartida
(`public.profiles`): Deliveries (base), HR (antes Recruiting; schema sigue
siendo `recruiting.*`), ERP (schema `erp.*`, 84K filas, Tailwind), Time Tracker
(schemas `timetracker.*` y `clockin.*`). Dueño: Andrés (opera en español; código
y comentarios en inglés). Último commit: `fd54237` (cierre de auditoría). Última
decisión: D-184.

## 2. PATRÓN DE N-MÓDULOS — todo es genérico, no lo rompas

En `src/lib/constants.ts`:

- `MODULES`: tarjetas de navegación (emoji/label/href) de los módulos opcionales.
- `HUB_TOOLS`: herramientas compartidas del hub (hoy: Usuarios), visibles por
  `visible(me)`, no por `module_access`.
- `MODULE_ACCESS`: descriptor por módulo para el diálogo de usuario —
  `{ key, alwaysOn, roleColumn, accessColumn?, capabilities? }`. Deliveries:
  `alwaysOn` (role NOT NULL, nadie se queda sin módulo), `roleColumn` `"role"`,
  `capabilities` (permissions). HR: `"recruiting_role"`. TT: `"timetracker_role"`.
  ERP: `"erp_role"` (staff|manager|admin). Test: `roleColumn` ÚNICO por módulo —
  nunca dos módulos a la misma columna. `UserDialog` despacha con switch
  EXHAUSTIVO por key (sin `default`): módulo nuevo sin caso = no compila.
- `accessibleModules(module_access)` → `[deliveries, ...módulos otorgados]`.
- `landingRoute(me)`: aterrizaje post-login (1 módulo → directo; 2+ → `/home`;
  driver → `/driver` SIEMPRE). No la parches; separa "aterrizaje" de
  "permanencia" (`hasReasonToBeHere` en `/home`).
- `canReachHub()`: UNA sola función para "¿ve el hub?" — el chofer se excluye
  ahí. NO dupliques esa regla en otro sitio (así se coló la regresión D-056).
- `ModuleSwitcher`: ícono ⇄ (2+ módulos) y ⌂ (`canReachHub`). Props
  `deliveriesRole` (NUNCA `"role"` a secas: dentro de HR, `me.role` es
  `recruiting_role`).
- `/home` = hub neutral: selector de módulos + herramientas. `/home/users` =
  gestión de usuarios (fuera de todo módulo); `/users` y `/recruiting/users`
  redirigen ahí.

Agregar un 5º módulo = entrada en `MODULES` + `MODULE_ACCESS` + su caso en el
switch + su columna de rol con guard + su route group. Nada más.

## 3. AISLAMIENTO

Cada módulo en su route group hermano con su propio layout (auth server-side +
redirect ANTES de montar). `(app)/` monta `DriverGate`/`LocationTracker` (GPS del
chofer) — ningún otro módulo debe heredarlo. Componentes de presentación puros
sí se comparten (`usePrefs`, `ModuleSwitcher`); providers con efectos NO. CSS:
Next emite un chunk por layout — el Tailwind del ERP (con preflight COMPLETO, no
uno "scoped" a mano) vive solo en su chunk; HR usa `.recruiting-module`.
Verifica el aislamiento midiendo chunks, no suponiendo.

## 4. IDENTIDAD COMPARTIDA — la clase de bug que más se repitió

Cinco bugs del merge y varios hallazgos de auditoría fueron LO MISMO: código de
un módulo tocando la fila/columna de otro en `profiles`. Reglas:

- Cada módulo escribe SOLO su columna desde SU función (`updateUserRole` →
  `role`; `updateUserRecruitingAccess` → `recruiting_role`/`module_access`; etc.).
  No fusiones funciones que D-053/D-057 separaron.
- Ningún módulo borra la cuenta de Auth: solo REVOCA su acceso.
- Toda lista de "usuarios" de un módulo filtra por su propio acceso.
- `.from("profiles")` lleva `.schema("public")` donde el cliente apunta a otro
  schema; canales realtime al schema correcto.

Base (autoridad real, la UI solo la refleja):

- `profiles`: RLS por fila (SELECT amplio; UPDATE self OR `is_admin()`; DELETE
  nunca desde cliente). Guards por columna: `role`,
  `recruiting_role`/`module_access`, `timetracker_role`, `erp_role`, y
  `permissions`/`store`/`username` (solo admin de Deliveries cambia cualquiera).
- `settings`: escritura `is_admin()`. `recruiting.settings`: admin+manager de HR
  (decisión consciente, D-180). `order_events`: append-only (INSERT con
  `created_by = auth.uid()`; sin UPDATE/DELETE ni para admin). `stage_history` e
  historial del ERP: solo triggers DEFINER.
- ERP: `erp.current_app_role()` lee `erp_role` (no el rol de Deliveries). Costo
  solo por la vista `app_products` con `can_see_cost()`; SELECT de tabla base
  revocado y re-otorgado por columna (un REVOKE de columna NO basta si hay SELECT
  de tabla). Solo Andrés y Roberto son erp admin; acceso nuevo nace `staff` (sin
  costo).
- `guard_delivery_stage` (etapas de órdenes) NO se toca.
- `schema_migrations`: ledger; toda migración se auto-registra tras el marcador
  `-- @ledger-below`; antes de aplicar, correr el status. 101 aplicadas, 0
  desfase.

## 5. REGLAS DE TRABAJO (CLAUDE.md + lo aprendido)

- NO ADIVINES, MIDE. Cada hallazgo con archivo:línea o consulta. (Origen: el
  preflight de Tailwind excluido por suposición rompió el ERP.)
- Cambio de comportamiento = PLAN EN PAPEL primero, el dueño aprueba, luego
  aplica. Bugs claros sin decisión de negocio (Clase A) se aplican y se listan.
- Antes de tocar RLS/guards en producción: `pg_dump` completo verificado con
  `pg_restore --list` (carpeta `C:\Users\andre\Documents\CLAUDE\RESPALDOS-DB\`).
  RLS no destruye datos; el riesgo es dejar a todos fuera.
- Pruebas de permisos: matriz rol × acción con `ROLLBACK` contra producción,
  usuarios REALES + sesiones SINTÉTICAS para combinaciones que hoy no existen
  (probar solo con Maximo, el único chofer, no probó el caso roto de D-056).
- Ninguna prueba dispara efectos en terceros (SMS, llamadas, correos, cuota de
  APIs). Stub, cuerpo saneado o sandbox. (Origen: un RingOut real, D-172.)
- Flujo por commit: `tsc --noEmit` · `vitest` · versión · `next build` · commit ·
  rebase · push · `DECISIONS.md` si cambia comportamiento. Versión: UI → package
  Y APP_VERSION; solo-base → solo `package.json` (excepción escrita).
- `DECISIONS.md` es el original de los ADR; escribe para quien no estuvo (qué
  fallaba, por qué, qué se descartó). El historial nunca se borra; una decisión
  que cambia se marca Reemplazada. `docs/decisiones.html` es la bitácora pública.
- Notion está PARQUEADO por instrucción verbal del dueño, en conflicto con
  D-046/regla 1 de `CLAUDE.md` — decisión escrita pendiente. No sincronices.
- El chofer NUNCA ve hub, switcher ni otro módulo; siempre `/driver`.
- El dueño recibe los prompts de un asistente revisor en otro chat; te pedirá
  planes en papel, tablas de verificación y "PARA" entre pasos. Reporta con
  tablas, cita commits, y confiesa errores propios en vez de enterrarlos.

## 6. ESTADO OPERATIVO

Crons: `/api/notion-summary` (Vercel 01:00 UTC, sigue aunque Notion esté
parqueado — revisar si debe seguir); `roll-schedules` de TT (Vercel 08:00 UTC);
cron de TT notificaciones + cierre 8 PM vía `.github/workflows/tt-cron.yml` cada
2 min — REQUIERE el secreto `TT_CRON_SECRET` en GitHub (= `CRON_SECRET` de
Vercel); hasta que el dueño lo cree, Actions marca rojo y el cierre automático NO
corre; GitHub pausa schedules tras 60 días sin actividad. Limpieza de fotos:
escrita, NO programada (destructiva, sin respaldo). Daltile: inactivo por 3 env
vars faltantes (`JOBS_SECRET`, `WAREHOUSE_CATALOG_URL`, `WAREHOUSE_READ_TOKEN`).
Planes gratis: Supabase (sin respaldos automáticos) y Vercel Hobby (1 cron/día).

## 7. DIFERIDO POR EL DUEÑO (con fecha 2026-09-03/04; abierto por decisión)

Token `sbp_` expuesto y vivo · password de la base (= ref del proyecto, débil,
expuesto en chat) · respaldos automáticos (F-3) · apps viejas de HR (apunta al
Supabase viejo `cfawfwzndxumeufhcwga`) y TT aún desplegadas · llave
`NEXT_PUBLIC_GOOGLE_MAPS_BROWSER_KEY` vacía en producción · env vars de Daltile.

NO los resuelvas por tu cuenta; recuérdalos una vez si tocan lo que estés
haciendo.

## 8. ABIERTO MENOR

A-2g: todos leen `permissions`/`username` de todos (fuga de información; cerrar
con vista, no con REVOKE, o rompe el diálogo de admin). 3 tests de PDFs saltados
(fixtures en `~/Downloads`, fuera del repo). Decisión de Notion.

## 9. LECCIONES QUE NO SE OLVIDAN

Una regla, un lugar (D-056). Preflight completo, no adivinado (ERP). REVOKE de
columna necesita revocar la tabla (A-2d). Reporta pasados|saltados, no un solo
número. Verificar contra el usuario que NO ejercita la ruta no prueba la ruta.
La UI no debe prometer lo que la base no hace (D-044, fotos "60 días").

---

## Verificación del 2026-09-04 (nota añadida al recibir el handoff)

Medido en la máquina el mismo día en que se escribió el documento. El texto de
arriba se guarda **tal cual llegó** (regla 2 de `CLAUDE.md`: el historial no se
reescribe, se anota). Lo que difiere:

| Punto | Lo que dice el handoff | Lo medido | Veredicto |
|---|---|---|---|
| §0 y §5 · versión | habla de un `APP_VERSION` en singular | no existe: es el mapa `APP_VERSIONS` por app en `src/lib/app-versions.ts` (D-087) — `deliveries 1.56.0`, `recruiting 0.13.1`, `timetracker 0.49.9`, `clockin 0.38.0`, `erp 0.3.13`; `package.json` 1.108.3 | **corregir al leer**: se sube la(s) app(s) tocada(s), no un contador global |
| §4 · ledger | "101 aplicadas, 0 desfase" | `migrate-status.mjs`: **102 en repo, 102 registradas, checksum coincide, todo al día**. La 102 es la migración del propio ledger (D-184), que se auto-inscribe | **desfasado por uno**, sin problema real |
| §0 · suite | 704 \| 3 = 707 | idéntico: 64 ficheros pasados, 1 saltado; **704 pasados \| 3 saltados** en 3.69 s | ✅ |
| §1 · último commit | `fd54237` | `fd54237 docs(audit): cierre de AUDIT-2026-09…`; rama `main`, árbol limpio, sincronizada con `origin/main` | ✅ |
| §1 · última decisión | D-184 | `DECISIONS.md` termina en D-184. Ojo: **no hay entrada propia D-178** — es el hueco anotado en el backfill (`5574dcb`) | ✅ |
| §2 · patrón de módulos | MODULES · HUB_TOOLS · MODULE_ACCESS · accessibleModules · landingRoute · canReachHub | los seis existen en `src/lib/constants.ts` (`canReachHub`:174, `landingRoute`:179, `MODULES`:236, `accessibleModules`:291, `HUB_TOOLS`:332, `MODULE_ACCESS`:640) con cuatro `roleColumn` distintos (`role`, `recruiting_role`, `timetracker_role`, `erp_role`:688) | ✅ |
| §5 · versión solo-base | "solo-base → solo `package.json` (excepción escrita)" | la excepción está literal en el paso 3 de `CLAUDE.md`, y además hay una sección que el handoff no lista como propia: **Registro de migraciones** (correr el status antes de aplicar; `-- @ledger-below`) | ✅ ampliado |

Nada de lo medido contradice el fondo del documento: las dos diferencias son de
número (101→102) y de nombre (`APP_VERSION` → `APP_VERSIONS`).
