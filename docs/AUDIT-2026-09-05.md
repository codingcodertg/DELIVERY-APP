# Auditoría general RTG Hub — 2026-09-05 (solo hallazgos)

Pedida por el dueño el 2026-09-05: *"another general audit… buttons, redirections,
aesthetics, translations, efficiently, the GPS that works as a tracker with the
APK, and even more"*. Solo lectura, sin arreglar nada; cada hallazgo con
archivo:línea, severidad y clase. Complementa `docs/AUDIT-2026-09.md` (cerrada el
04-09). Informe del agente de exploración, transcrito íntegro por el orquestador
(el transcript del agente no se persistió).

> **Corrección al recibir el informe (orquestador, 2026-09-05):** el punto (c)
> dice que no pudo confirmar `TT_CRON_SECRET`. Sí existe desde el 04-09 y el
> workflow `tt-clockin-cron` está en verde (D-182/D-183 y CLAUDE.md). Se anota
> aquí; el informe no se reescribe.

---

**Checkout:** `main` @ `077322c` · repo v1.115.1 · deliveries 1.59.0 / recruiting 0.14.0 / timetracker 0.57.0 / clockin 0.39.0 / erp 0.4.0 (confirmado contra `GET https://rtg-hub.vercel.app/api/version`).
**Método:** lectura de `docs/AUDIT-2026-09.md` y `docs/HANDOFF-2026-09-04.md` primero, para no repetir; inventario por script sobre `src/`, `node scripts/verify.mjs` completo (tsc + 757 pasados | 3 saltados + `next build` con tamaños por ruta), `npm audit --omit=dev`, y `curl` **solo GET a páginas públicas sin sesión**. No se llamó a `/api/notify`, `/api/call`, `/api/help` ni a ningún cron. No se escribió nada.
**Clase A** = bug claro, sin decisión de negocio. **Clase B** = necesita decisión.

---

## 1. RUTAS Y REDIRECCIONES

Inventario: **72 `page.tsx`**, **30 `route.ts`**, **7 layouts**, **21 redirects** en `next.config.mjs`. Puertas de sesión: los 4 layouts (`(app)`, `recruiting/(recruiting)`, `timetracker/(timetracker)`, `erp`) + `src/middleware.ts` (solo refresca sesión) + gate por página en las 19 páginas del ERP. Comprobado con curl: todas las rutas protegidas dan **307 → /login**; `/login`, `/api/version`, `/track/[id]`, `/manifest.webmanifest` dan 200. Enlaces de `TABS`/`MANAGER_TABS`/`MODULES`/`HUB_TOOLS`/`side-nav`: **todos apuntan a rutas existentes** (verificado uno a uno).

**G-1 · P1 · Clase A · ERP · `src/app/erp/` (falta `page.tsx`)**
`https://rtg-hub.vercel.app/erp` responde **404**. Medido: `curl -o /dev/null -w '%{http_code}'` → `404`. `src/app/erp/layout.tsx:33` redirige al no autenticado a `/login?next=/erp/catalog`, y `src/app/erp/analytics/page.tsx` sí existe justo para esto (`redirect("/erp/analytics/stores")`). Falta el equivalente en la raíz del módulo.

**G-2 · P2 · Clase A · base · `src/app/(app)/layout.tsx:26,46`**
`redirect("/login")` **sin `?next=`**, mientras que `erp/layout.tsx:33` y `timetracker/(timetracker)/layout.tsx:38` sí lo ponen. Medido: `/driver` y `/users` → `/login` (pierde el destino); `/home`, `/timetracker`, `/erp/catalog` → `/login?next=…`. El login honra `next` (`src/app/login/page.tsx:57,152`). Duele sobre todo en el escritorio de Electron, sin barra de direcciones (misma razón que D-076).

**G-3 · P2 · Clase A · TT/fichaje · `next.config.mjs:34` + `:84`**
**Redirecciones encadenadas de tres saltos.** Medido: `curl -L -w '%{num_redirects}'` sobre `/clock-in/clock` → **3 redirecciones**, final `/login?next=/timetracker`. La regla comodín `"/clock-in/:path*" → "/timetracker/clock-in/:path*"` desemboca en otra regla (`:84`) que va a `/timetracker`, y luego el gate del layout. El comentario de `:66-70` dice que apuntan directo "para no encadenar dos redirecciones en un enlace que abre gente desde el móvil"; la regla comodín rompe esa intención. Arreglo: reglas explícitas `"/clock-in/clock" → "/timetracker"` etc., antes del comodín.

**G-4 · P3 · Clase B · HR · `src/app/recruiting/(recruiting)/users/page.tsx`**
La página-redirección a `/home/users` vive **dentro** del route group `(recruiting)`, así que el layout la gatea por `recruiting_role` **antes** de que el `redirect()` corra. Medido: `/recruiting/users` sin sesión → `/login?next=/recruiting`, no `/home/users`. Comparar con `src/app/(app)/users/page.tsx`, que sí funciona.

**G-5 · P2 · Clase B · base · `src/app/(app)/approvals/page.tsx`**
Página completa (137 líneas, gateada por `canApprove`) **sin un solo enlace en toda la app**: 0 referencias a `/approvals` fuera de su fichero, no está en `TABS` (`src/lib/constants.ts:85-120`), existe desde el commit inicial (`8e2206d 2026-07-17`). O se enlaza o se borra.

---

## 2. BOTONES Y ACCIONES

Medido: **0 `console.log`**, **0 `onClick={() => {}}`**, **0 `href="#"`**, **0 TODO/FIXME reales**. La mayoría de acciones destructivas sí confirman (`CandidateRow.tsx:271`, `AssignmentsPanel.tsx:146`, `routes/page.tsx:2411`, `diary/page.tsx:20`).

**G-6 · P2 · Clase A · ERP · `src/components/erp/saved-views.tsx:44-47`**
`del(id)` borra permanentemente **sin confirmación** (botón `×` pegado al nombre, línea 73) y **sin mirar el error**: si RLS rechaza el borrado, la vista reaparece al refrescar y el usuario no ve nada. Usado en `catalog-table.tsx:20` y `review-queue.tsx:14`.

**G-7 · P3 · Clase A · base · `src/components/ShiftClock.tsx:66-67`**
`doIn`/`doOut` sin `try/finally`: `clockIn`/`clockOut` (`data-provider.tsx:1256,1265`) terminan en `await reloadAll()`, que puede rechazar en un fallo de red. Si rechaza, `busy` queda `true` y el botón de fichar del chofer se queda muerto hasta recargar, y ese botón es el interruptor del GPS (§6).

**G-8 · P2 · Clase B · base · `data-provider.tsx:1265-1271` + `src/lib/outbox.ts:15-18`**
`clockOut` **sin red no encola**: el `outbox` es "deliberadamente estrecho: solo los hitos del chofer". El chofer que marca salida sin cobertura ve un toast rojo, el turno sigue abierto en la base y **el servicio de GPS sigue reportando**. Decisión: ¿entra el fichaje en la cola, o se cierra el turno desde el servidor?

---

## 3. TRADUCCIONES

**Medido por script sobre `src/lib/timetracker/i18n.ts`:** 554 claves en `en`, **554 en `es`**, **0 claves en un idioma y no en el otro**, **0 claves usadas que no existan**. `src/lib/timetracker/i18n.test.ts` cubre interpolación, etiquetas de pestaña, 3 claves de "Registrar tiempo" y **10 ficheros listados a mano** (`:90-103`). **No cubre** los otros ~36 ficheros de TT ni los otros módulos, ni detecta texto inglés a pelo.

**G-9 · P2 · Clase B · TT · pantallas ENTERAS sin traducir**
21 de 46 ficheros de TT no importan `useT`/`usePrefs`. De cara al empleado, 100 % en inglés:

| Fichero | Evidencia |
|---|---|
| `src/app/timetracker/(timetracker)/account/page.tsx` | `:40` `My account`, `:41` `Saved.`, `:55` `How I get paid`, `:71` `My setup`, `:81` `Notifications`, `:108` `Change password`, `:136` `Devices` — la pantalla entera |
| `src/app/timetracker/(timetracker)/requests/page.tsx` | `:144` `New request`, `:157` `Project`, `:159/180/189/203/224` `Pick…`, `:201` `Entry to fix`, `:214-215` — ~14 cadenas, mitad y mitad |
| `src/app/timetracker/(timetracker)/week/page.tsx` | `:80` `Next →`, `:87` `Paid this week:`, `:90-92`, `:96`, `:101-105`, `:125` — ~12 |
| `ClockinApprovals.tsx`, `ExceptionHistory.tsx`, `GeofenceEditor.tsx` (~13), `GeofenceSection.tsx`, `TripPanel.tsx` (~11), `VehiclesSection.tsx`, `EmployeeWeek.tsx`, `DayPhotos.tsx` (~6), `OfflineIndicator.tsx`, `UpdateBanner.tsx`, `NotificationBell.tsx`, `CrewMap.tsx` | 0 llamadas a `useT` |

Total aproximado TT fuera de `t()`: **~166 cadenas**.

**G-10 · P2 · Clase B · ERP · módulo entero sin i18n**
`grep -rn 'usePrefs\|useT(' src/app/erp src/components/erp src/lib/erp` → **0 en 64 ficheros**. **~346 cadenas** en inglés. Peores: `product/[id]/page.tsx` (~69), `inventory-console.tsx` (~23), `receiving.tsx` (~16), `request-form.tsx` (~16), `po-reconcile.tsx` (~15). `side-nav.tsx` **sin conmutador de idioma**. Es el D-3 de la auditoría anterior, abierto: decisión pendiente (¿se traduce o se declara "inglés a propósito"?).

**G-11 · P3 · Clase A · TT · `src/lib/timetracker/i18n.ts` — claves muertas**
139 claves sin uso (descontando `tab.*`). Las conocidas: `mgr.pay.onSite`, `mgr.pay.remote`, `mgr.rep.prev`, `mgr.rep.next`, `tab.team-diary`. Bloques enteros huérfanos: `auth.*` (17, login propio del TT original), `mgr.usr.*` (23, gestión de usuarios sustituida por `/home/users`), `mgr.set.dataBackup`/`backupNote`/`download`/`restoreBtn`/`backupDone`/`restoreConfirm`/`restoring`/`restoreDone`/`restoreFail` (**una pantalla de copia y restauración que el diccionario promete y el código no tiene**; F-3 sigue siendo "sin ningún respaldo"), `mgr.start.*` (12), `theme.*`, `mgr.ppl.delete*`. `tab.schedule` conservada a propósito (`i18n.ts:56-57`).

**Base y HR:** base ~56 cadenas fuera de `t()` (mayoría falsos positivos en `data-provider.tsx`; las reales en `routes/page.tsx` y `account/page.tsx`). HR ~39, casi todas falsos positivos; 13 de 16 ficheros usan `usePrefs`. Sanos.

---

## 4. ESTÉTICA / CONSISTENCIA

**G-12 · P1 · Clase A · global · `src/app/manifest.ts:13` vs `public/manifest.webmanifest`**
**Dos manifiestos para la misma URL, y gana el que no tiene iconos.** Medido: `curl https://rtg-hub.vercel.app/manifest.webmanifest` devuelve **el generado por `src/app/manifest.ts`** (`"icons":[]`), y `next build` lo lista como ruta `○ /manifest.webmanifest`. `public/manifest.webmanifest`, con iconos, `orientation` y descripción, **está muerto**. "Añadir a pantalla de inicio" no tiene icono. Borrar `src/app/manifest.ts` deja servir el correcto.

> Nota del orquestador: contradice la medición de D-188, que dio que ganaba el de `public/`. Se mide otra vez al aplicar (lote 1) y se deja escrito cuál gana y por qué.

**G-13 · P3 · Clase B · global · colores a pelo**
604 literales `#hex`/`rgb()` en 61 ficheros. Los 322 de los cuatro CSS son legítimos (definen variables). **~140 dentro de `style={{…}}` en TSX**: `routes/page.tsx` (36), `OrderModal.tsx` (31), `map/page.tsx` (13), `market/page.tsx` (12), `LeafletMap.tsx` (11), `recruiting/ModalHost.tsx` (11), `timetracker/TopBar.tsx` (9). No cambian con el tema. Ejemplo: `ShiftClock.tsx:76-77` mezcla `var(--green)` y `rgba(16,185,129,0.06)`.

**G-14 · P3 · Clase B · HR · `src/app/recruiting/recruiting.css:224`**
Tema oscuro casi sin cubrir: **1 sola regla `[data-theme="dark"]`** en 257 líneas (15 en `globals.css`, 17 en `timetracker.css`). Es D-4 anterior, sin cambios. Cerrados y verificados: D-2 (barra del ERP, `side-nav.tsx:193`) y B-8 (rótulos del rename).

---

## 5. EFICIENCIA

`next build`: **First Load JS compartido = 190 kB**. Rutas más pesadas: **`/` 588 kB**, `/routes` 347 kB, `/driver` 328 kB, `/map` 326 kB, `/warehouse` 326 kB, `/summary` 325 kB, `/erp/review` 304 kB, `/erp/catalog` 304 kB. Middleware 102 kB.

**G-15 · P2 · Clase B · base · `src/lib/data-provider.tsx:600-620`**
**Realtime que recarga todo por cualquier cambio.** El canal engancha ocho tablas, todas a `scheduleReload` → `reloadAll()`, que **vuelve a bajar las 9 consultas** de `:470-493` (incluida `deliveries.select("*")` entera). Un chofer marcando "entregado" dispara dos veces, con debounce de 250 ms, en **todas** las sesiones abiertas. `driver_locations` sí está resuelto bien (`:623`).

**G-16 · P2 · Clase A · base · `src/lib/data-provider.tsx:475`**
`deliveries.select("*")…order("order_no")` **sin `.limit()` ni `.range()`**, en cada carga y en cada `reloadAll`. Contrasta con `:480`, donde `order_events` sí se acotó a `EVENTS_WINDOW` con comentario ("855 filas / 376 kB").

**G-17 · P2 · Clase A · TT · `src/lib/timetracker-data-provider.tsx:272,488`**
`sessions.select("*")` y `screenshots.select("*")` por empleado **sin cota temporal ni límite**; el canal realtime de `:495` llama al mismo `load()` en cada `*`, o sea **cada ~10 min durante el turno se rebaja todo el histórico** para añadir una fila.

**G-18 · P2 · Clase A · base · `src/app/(app)/track/page.tsx:88-97`**
Para la tira de "qué días tienen datos" se bajan hasta **20.000 filas** de `driver_locations` y se agrupan en el navegador. Es una agregación de una línea en SQL o un RPC.

**G-19 · P3 · Clase A · HR · `recruiting-data-provider.tsx:557,584,769`**
Tres bucles con `await` a Supabase por iteración (`duplicateQuestion`, `reorderQuestions` —un UPDATE por id arrastrado—, `addStage`). Candidatos a un `upsert` por lotes.

**G-20 · P3 · Clase B · global · `next build`**
`/` con 588 kB es el doble de cualquier otra ruta. Causa: `OrderModal.tsx` (3.122 líneas) y `routes/page.tsx` (2.544) sin `dynamic()` (E-2 anterior, sigue igual).

**G-21 · P2 · Clase B · dependencias · `npm audit --omit=dev --registry=https://registry.npmjs.org`**
**6 vulnerabilidades en producción (3 altas, 3 moderadas):** `fast-uri` 3.0.0–3.1.5 (4 avisos, SSRF; **arreglable con `npm audit fix`**); `postcss ≤8.5.22` (4 avisos, vía `next`; arreglo declarado `next@16.3.4`, cambio mayor); `uuid <11.1.1` (moderada, vía `exceljs`). Nota: `npm audit` sin `--registry` falla en esta máquina ("426 Upgrade Required", el `.npmrc` usa `http://`), así que un `npm audit` normal **parece limpio y no lo está**.

---

## 6. GPS / TRACKER / APK

**Lo que sí funciona:** no es "solo `watchPosition`". `mobile/package.json` incluye `@capacitor-community/background-geolocation ^1.2.22`; `AndroidManifest.xml` declara `ACCESS_BACKGROUND_LOCATION`, `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_LOCATION`, `WAKE_LOCK`, `POST_NOTIFICATIONS`, `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`; `src/lib/useLiveLocation.ts:180-198` arranca el servicio en primer plano con notificación permanente; plugin propio `BatteryGuard` (`native-bridge.ts:75-88`); `DriverGate.tsx` bloquea la entrada hasta tener permisos; `LocationTracker` montado en el layout (`(app)/layout.tsx:60`). Encendido/apagado por turno, con `device_id`. Frecuencia: mínimo 10 s entre escrituras, 25 m de movimiento, latido cada 5 min parado, ofertas nativas cada 60 s. URL del APK correcta (`capacitor.config.ts:20`); la vieja redirige 307, ni los APK viejos se rompen. Versión APK coherente: `versionCode 4` = `RDZDeliveries/4` = `LATEST_APK_VERSION_CODE = 4` = `/api/version` `"apk":4`.

**G-22 · P1 · Clase B · GPS · `src/lib/data-provider.tsx:872-887` + `src/lib/outbox.ts:15-18`**
**Sin red, la fijación se pierde y no hay cola.** `pushLocation` hace `insert` y, si hay error, `return false`. El `rtg_outbox` **no cubre GPS**. En zonas muertas **el recorrido tiene agujeros que luego no se pueden reconstruir**, aunque el teléfono capturó las posiciones. Además el latido de `useLiveLocation.ts:112` actualiza `lastRef.current` antes de saber si el envío funcionó (`:123-128`), así que un latido fallido consume su ventana de 5 min.

**G-23 · P1 · Clase B · GPS · `supabase/migrations/043_driver_locations.sql:59` — sin retención**
`public.prune_driver_locations(keep_days int default 30)` existe y **nadie la llama**: cero en `src`, `scripts`, `.github`, `vercel.json`. A ~170 filas/día/chofer la tabla crece sin techo, con realtime encima. Misma clase que C-6 y `cleanup-photos`.

**G-24 · P2 · Clase A · GPS · `src/app/(app)/track/page.tsx:64,65,95` — desfase por horario de verano**
Límites del día con **offset fijo `-05:00`** (`${date}T00:00:00-05:00`), mientras `:102` clasifica con `America/Chicago`. De noviembre a marzo (CST, `-06:00`) **las fijaciones entre las 23:00 y la medianoche caen en el día equivocado**. `src/lib/clockin/tz.ts:1` ya es "DST-aware Central Time".

**G-25 · P2 · Clase A · TT · zona horaria configurable que la mitad del código ignora**
`settings/page.tsx:142-145` ofrece selector de zona y `helpers.ts:113` lo respeta, pero **seis componentes cablean `"America/Chicago"`**: `PayrollTimesheets.tsx:38,40`, `PunchPanel.tsx:77`, `TripPanel.tsx:39`, `EmployeeWeek.tsx:21`, `DayPhotos.tsx:104`, y `clock-in/api/reports/{export,xlsx}/route.ts:20,23,25,28`. Con otra zona, las **fechas** cambian y las **horas** no, en la hoja de horas y en el Excel de nómina.

**G-26 · P2 · Clase A · GPS · `src/components/LocationTracker.tsx:53` — comentario que promete lo que el código no hace (D-044)**
`useLiveLocation.ts:15-17` dice que en navegador cae a la geolocalización de la página, y `:207-227` lo implementa; pero `LocationTracker.tsx:53` termina en `return isNativeApp();`: **el hook nunca se activa fuera del APK**. Un chofer en Chrome del móvil no reporta nada y nada se lo dice (`status` = `"off"`).

**G-27 · P3 · Clase A · GPS · comentarios desactualizados en `src/lib/useLiveLocation.ts`**
`:99` y `:139` dicen "40 m", pero `MIN_MOVE_M = 25` (`location-filter.ts:33`); `:29-31` habla de "dos minutos", pero `TIMED_INTERVAL_MS = 60_000` (`:37`); `:26-28` estima "~100 filas/día", `location-filter.ts:21` calcula ~170.

**G-28 · P3 · Clase A · TT · `src/app/timetracker/(timetracker)/layout.tsx:33-37` — comentario falso**
Dice que el middleware "es código muerto porque vive en la raíz": el middleware se movió a `src/middleware.ts` en D-119 y `middleware-location.test.ts` lo vigila.

**G-29 · P3 · Clase B · `src/lib/supabase/middleware.ts:111-178` — guard de rutas escrito y desconectado**
`updateSession` (68 líneas) exportado y **no lo llama nadie**; `src/middleware.ts:20` solo invoca `refreshSession`. Documentado como "paso 2"; merece decisión: conectarlo (resolvería G-2) o retirarlo.

---

## 7. Y MÁS

**G-30 · P3 · Clase A · ficheros muertos · `src/app/timetracker/clock-in/actions/{consent,runReview,tutorial}.ts`**
Tres server actions sin ningún importador (barrido de `src/`). Restos de D-137. Mismo tipo que E-3 (cerrado en D-176).

**G-31 · P3 · Clase A · versiones · `package.json`**
`@types/web-push` sigue en `dependencies` (F-2 anterior, no aplicado). `APP_VERSIONS.clockin` (`0.39.0`) sigue siendo la versión de un módulo borrado en D-137, y `/api/version` la publica.

**Pruebas — sin tautologías.** `i18n.test.ts` (D-187) extrae las claves **del código fuente** y las contrasta con `DICT`; su límite es de cobertura, no de rigor (G-9). Suite: **757 pasados | 3 saltados** (los 3 de `pdf.test.ts`, fixtures fuera del repo).

**Secretos:** solo la clave Firebase de cliente ya conocida (`google-services.json:18`, A-1b, va en el APK por diseño). **Sin secretos nuevos.** URLs cableadas correctas tras el rename.

**Diferidos del dueño que siguen abiertos** (citados una vez): token `sbp_` vivo, contraseña de la base débil y expuesta, **cero respaldos** (F-3; y G-11 muestra nueve claves traducidas de una pantalla de copia/restauración que no existe), apps viejas de HR/TT desplegadas, `NEXT_PUBLIC_GOOGLE_MAPS_BROWSER_KEY` vacía en producción.

---

## (a) Resumen por severidad y clase

| Sev. | Clase A | Clase B | Total |
|---|---|---|---|
| **P0** | — | — | **0** |
| **P1** | G-1, G-12 | G-22, G-23 | **4** |
| **P2** | G-2, G-3, G-6, G-16, G-17, G-18, G-24, G-25, G-26 | G-5, G-8, G-9, G-10, G-15, G-21 | **15** |
| **P3** | G-7, G-11, G-19, G-27, G-28, G-30, G-31 | G-4, G-13, G-14, G-20, G-29 | **12** |
| | **18 A** | **13 B** | **31** |

Ningún P0 nuevo. Los P0 vivos son los **diferidos por el dueño**.

## (b) Los 10 que arreglaría primero

1. **G-12** manifest sin iconos (P1, A): un fichero a borrar y el icono de la PWA vuelve.
2. **G-1** `/erp` da 404 (P1, A): cuatro líneas.
3. **G-23** `driver_locations` sin retención (P1, B): la función existe; falta programarla. Es B porque borra datos y no hay respaldo (F-3).
4. **G-22** el GPS pierde fijaciones sin red (P1, B): lo que más daña la palabra "tracker".
5. **G-24** el `-05:00` cableado en `/track` (P2, A): muerde en noviembre.
6. **G-25** la zona horaria que la nómina ignora (P2, A): seis sustituciones; afecta a recibos.
7. **G-6** borrado sin confirmar en el ERP (P2, A).
8. **G-2** `/login` sin `next` en la mitad de la app (P2, A); con G-29 desaparecería sola.
9. **G-15 + G-16** el realtime que recarga todo (P2, B): el mayor ahorro; B porque cambia cómo se refresca.
10. **G-26** el comentario que promete respaldo en navegador (P2, A): barato y peligroso de no arreglar.

## (c) Lo que NO pude medir, y por qué

- **El GPS en un teléfono real.** Todo §6 es lectura de código y configuración; no se verificó en un Android físico (pantalla apagada, segundo plano, zona sin cobertura, `BatteryGuard` en Xiaomi/Samsung). El propio código admite fijaciones con más de una hora de retraso (`useLiveLocation.ts:114-119`). Se mide con un turno real y un `select` sobre `driver_locations` comparando `recorded_at` con `created_at`.
- **La base de datos.** Sin credenciales cargadas: las cifras de volumen son las de `docs/AUDIT-2026-09.md` del 03-09. G-16/17/18 son de forma de consulta, no de tamaño medido hoy.
- **Aspecto visual real.** Sin navegador: G-13/G-14 son conteos de fuente, no capturas. Sigue siendo el punto ciego de "estética" (D-5 anterior).
- **Las rutas con sesión.** Solo GET sin sesión: códigos y cadenas de redirección, no contenido tras login.
- **Sentry.** El token es de subida; no se leen incidencias abiertas.
